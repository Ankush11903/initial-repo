//creating a dashboard component for admin to add rates based on weight and distance

import React from 'react'

function Dashboard() {
  return (
    <div>Dashboard</div>
  )
}

export default Dashboard
